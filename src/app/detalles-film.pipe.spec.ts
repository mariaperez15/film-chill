import { DetallesFilmPipe } from './detalles-film.pipe';

describe('DetallesFilmPipe', () => {
  it('create an instance', () => {
    const pipe = new DetallesFilmPipe();
    expect(pipe).toBeTruthy();
  });
});
